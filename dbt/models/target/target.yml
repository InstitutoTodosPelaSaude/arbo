version: 2

sources:
    - name: dagster
      description: Import raw data from lab
      schema: arboviroses
      tables:
        - name: target_raw
          meta: 
            dagster:
              asset_key: ["target_raw"]