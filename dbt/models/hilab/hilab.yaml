version: 2

sources:
  - name: dagster
    description: Raw data from Hilab
    schema: arboviroses
    tables:
      - name: hilab_raw
        meta:
          dagster:
            asset_key: ["hilab_raw"]
